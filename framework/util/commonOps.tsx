export let associateFuncsWithThisArgs = function (thisArg: any) {
  // let getMethods = (obj: any) => Object.getOwnPropertyNames(obj).filter(item => typeof (obj[item]) === 'function');
  // for (let method in thisArg) {
  //   console.log(method);
  //   ((thisArg as any)[method]).thisArg = thisArg;
  // }
};