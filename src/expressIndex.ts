import { ServerManager } from '../framework/managers/serverManager';
import { Request, Response } from 'express';

// let app = ServerManager.app;
// let io = ServerManager.io;

// In electron mode, hits http://127.0.0.1:3000/test/
// app.get('/test/', (req: Request, res: Response) => { console.log("TEST ENDPOINT WORKED!") });